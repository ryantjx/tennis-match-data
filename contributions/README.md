# Community corrections

All proposed corrections live in `corrections.parquet`. Append a deterministic
record with `open-tennis-data add-correction --help`. Contributors dedicate the
factual correction to CC0 and provide a reviewable HTTP(S) evidence URL.
